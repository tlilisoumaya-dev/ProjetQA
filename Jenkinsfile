pipeline {

    agent any 

    stages {
        stage ('install requirements') {
            steps {
                echo 'hello'
                sh 'pip install -r requirements.txt'                
            }
        }
    }
}